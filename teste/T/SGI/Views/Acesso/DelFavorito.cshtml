@model SGI.Models.T_Favoritos

<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Remover grupo da meta</h4>
    </div>
    @using (Html.BeginForm("DelFavorito", "Acesso", FormMethod.Post, new { id = "formDados", name = "formDados" }))
    {
        <div class="modal-body">
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            @Html.HiddenFor(x => x.ID_INDICADOR)
            @Html.HiddenFor(x => x.ID_USUARIO)
            @Html.HiddenFor(x => x.IDFAVORITO)
            <div class="row">
                <div class="col-md-8 col-sm-9 col-xs-10">
                    <div class="form-group">
                        <label class="form-label">Indicador</label>
                        <div class="controls">
                            @Html.TextBoxFor(x => x.T_Indicadores.IND_DESCRICAO, new { @class = "form-control", @readonly = true })
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <div class="modal-footer text-center">
            <button data-dismiss="modal" class="btn btn-default" type="button">Fechar</button>
            <button class="btn btn-success" type="submit">Confirmar</button>
        </div>
    }
</div>

<script type="text/javascript">
    $('#formDados').submit(function () { // catch the form's submit event
        $.ajax({ // create an AJAX call...
            data: $(this).serialize(), // get the form data
            type: $(this).attr('method'), // GET or POST
            url: $(this).attr('action'), // the file to call
            success: function (response) { // on success..
                location.reload();
            },
            error: function()
            {
                location.reload();
            }
        });
        return false; // cancel original event to prevent form submitting
    });
</script>